---
name: vdoc
description: "Generate and maintain feature-centric product documentation"
invokable: true
---

# /vdoc — Documentation Generator

Run documentation workflows for this project.

**Arguments:** $ARGUMENTS

## If `init` or no `vdocs/` folder exists:

Read and follow the workflow at `.continue/references/vdoc/init-workflow.md`. Use `.continue/references/vdoc/doc-template.md` as the doc template and `.continue/references/vdoc/manifest-schema.json` for the manifest format.

## If `audit` or `vdocs/` already exists:

Read and follow the workflow at `.continue/references/vdoc/audit-workflow.md`.
